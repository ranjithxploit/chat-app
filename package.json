{
  "name": "chill-chat-vault",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run start:frontend",
    "start:frontend": "cd frontend && npm install && npm run dev",
    "build:frontend": "cd frontend && npm install && npm run build",
    "start:backend": "cd backend && npm install && npm start"
  }
}